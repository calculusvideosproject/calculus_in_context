%!PS-Adobe-2.0 EPSF-2.0
%%BoundingBox: 0 0 612.0 792
/printscale 1 def
/recip {1 printscale div} def
/screenstretch {printscale 90 50 div mul} def
/double {screenstretch 2 mul} def

% 100 300 translate 		% print translation

 printscale printscale scale       %scale for printing
% screenstretch screenstretch scale %printing scale on screen
% double double scale               %magnified screen

% -------------------- font codes --------------------------

/typesize {10 printscale div} def
/smalltype {8 printscale div} def

/makeTR {/Times-Roman findfont typesize scalefont} def
/makeTB {/Times-Bold findfont typesize scalefont} def
/makeTI {/Times-Italic findfont typesize scalefont} def
/makeSYM {/Symbol findfont typesize scalefont} def

/maketr {/Times-Roman findfont smalltype scalefont} def
/maketi {/Times-Italic findfont smalltype scalefont} def
/makesym {/Symbol findfont smalltype scalefont} def

/TR {makeTR setfont} def
/TB {makeTB setfont} def
/TI {makeTI setfont} def
/SYM {makeSYM setfont} def

/tr {maketr setfont} def
/ti {maketi setfont} def
/sym {makesym setfont} def

% ---------------string placing procedures-------------------

% stack: fontcode string x y 

/placeBL {moveto show} def
/placeBR {moveto dup stringwidth pop neg 0 rmoveto show} def
/placeBC {moveto dup stringwidth pop 2 div neg 0 rmoveto show} def
/placeTL {moveto 0 typesize neg rmoveto show} def
/placeTR {moveto dup stringwidth pop neg typesize neg rmoveto show} def
/placeTC {moveto dup stringwidth pop 2 div neg typesize neg rmoveto show} def

% -------------------- Ken's arrow drawing procedure --------------

/arrow
 {/mtrx matrix def
   /headlength exch def
   /halfheadthickness exch 2 div def
   /halfthickness exch 2 div def
   /tipy exch def /tipx exch def
   /taily exch def /tailx exch def
   
   /dx tipx tailx sub def
   /dy tipy taily sub def
   /arrowlength dx dx mul dy dy mul add sqrt def
   dx 0 ne {/angle dy dx atan def}
   	{dy 0 ne {/angle 90 dy dy abs div mul def}{/angle 0 def} ifelse}
	 ifelse
   /base arrowlength headlength sub def
   
   /savematrix mtrx currentmatrix def
   
   tailx taily translate
   angle rotate
   0 halfthickness neg moveto
   base halfthickness neg lineto
   base halfheadthickness neg lineto
   arrowlength 0 lineto
   base halfheadthickness lineto
   base halfthickness lineto
   0 halfthickness lineto
   closepath
   
   savematrix setmatrix
 }  def
 
% -------------------------- program ----------------------------

.3 setlinewidth

 /Sc   %scales points
 {vscale mul exch hscale mul exch} def
 
  /Sclog   %scales points
 { log vscale mul exch log hscale mul exch} def
 
 
 /Schead
 {/dx rtip x sub hscale mul def
 /dy ftip y sub vscale mul def
 /s dx dx mul dy dy mul add sqrt .1 mul def
 s mul exch s mul exch} def
 
 /model
 {/xp a x 1 x b div sub mul mul c x y x d add div mul mul sub def
 /yp e y 1 y f x mul div sub mul mul def
 /x x xp step mul add def
 /y y yp step mul add def
 x y Sc lineto
 } def
 
/horgrid
{/number exch def
/change ymax number idiv def
/k 0 def
number {hashver} repeat
} def

/vergrid
{/number exch def
/change xmax number idiv def
/k 0 def
number {hashhor} repeat
} def

/hashver
{/k k change add def
/posit k vscale mul def
k (            ) cvs dup stringwidth pop 7 add neg 
2 add posit 2 sub moveto
show
-2 posit moveto 
0 posit lineto stroke
} def

/hashhor
{/k k change add def
/posit k hscale mul def
k (     ) cvs dup stringwidth pop 2 div
posit exch sub -10 moveto
show
posit -2 moveto
posit 0 lineto stroke
} def

/model1
{/xp .15 x mul 1 .005 x mul sub .01 y mul sub mul def
 /yp .03 y mul 1 .006 x mul sub .005 y mul sub mul def
/x x xp step mul add def
/y y yp step mul add def
x y Sc lineto } def

/manyrun1
{/inity exch def
/initx exch def
/nrun exch def
/x initx def
/y inity def
x y Sc moveto
nrun {model} repeat
stroke
/sign xp xp abs div def
/rat yp xp div def
/xtip x .1 sign mul add def
/ytip y .1 sign rat mul mul add def
x y Sc xtip ytip Sc .1 3 5 arrow fill
x y Sc moveto
totalsteps {model} repeat
stroke
} def

/manyrun
{/x 4 def
/y 5 def
x y Sc moveto
450 {model} repeat
newpath
/sign xp xp abs div def
/rat yp xp div def
/xtip x .1 sign mul add def
/ytip y .1 sign rat mul mul add def
%x y Sc xtip ytip Sc .1 3 5 arrow fill
newpath
x y Sc moveto
totalsteps 100 add {model} repeat
%stroke
/sign xp xp abs div def
/rat yp xp div def
/xtip x .1 sign mul add def
/ytip y .1 sign rat mul mul add def
%x y Sc xtip ytip Sc .1 3 5 arrow fill
} def

 /fntsize 12 def
 /totalsteps 1500 def				% 1500 def
 /hscale 25 def  
 /vscale 15 def
    
  /tscale 1.2 def
 /xmax 10 def
 /ymax 10 def
 /tmax 400 def
 /step tmax totalsteps div def
 /fac 10 xmax totalsteps div exp def
 /ofac fac 1 sub def
 
 /a .6 def
 /b 10 def
 /c .5 def
 /d 1 def
 /e .1 def
 /f 2 def
 
 
60 20 translate


newpath
 0 0 0 ymax vscale mul 5 sub .01 2 4 arrow fill
 0 0 xmax hscale mul 20 add 0 .15 2 4 arrow fill
 
TI (x) xmax hscale mul 20 add 1 placeTC
(y) -4 ymax vscale mul 8 sub placeBR
sym
4 horgrid
5 vergrid
 
 gsave
 newpath
0 0 moveto 
xmax hscale mul 0 rlineto
0 ymax vscale mul rlineto
xmax hscale mul neg 0 rlineto
closepath
clip

newpath
TR (the limit cycle) 120 130 placeBL
140 128 moveto
120 76 lineto
stroke

newpath
.7 recip mul setlinewidth
190 1.1 2.23 manyrun1
%%4 1 1 manyrun1
%%4 8 8 manyrun1
92 2 3 manyrun1
4 10 7 manyrun1
4 10 4.5 manyrun1
4 10 2 manyrun1
28 .1 .1 manyrun1

gsave
2 recip mul setlinewidth
manyrun
stroke
grestore

gsave
1 setgray
1 recip mul setlinewidth
manyrun
stroke
grestore

gsave
[2 4] 0 setdash
1 recip mul setlinewidth
manyrun
stroke
grestore


grestore 

showpage
